# Copy to .env.local — the frontend reads API_URL at build/dev time and
# uses it to proxy /api/* requests to the FastAPI backend.
# Default (when unset) is http://localhost:8001 — the project's canonical port.
API_URL=http://localhost:8001
